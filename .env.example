# Neon pooled connection string (Preview / local). Never commit real values.
DATABASE_URL=

# Shared secret for /api/admin/* (Authorization: Bearer <key>)
ADMIN_API_KEY=

# Unlisted ops bookings page: /ops/<OPS_SECRET> or /ops?key=<OPS_SECRET>
OPS_SECRET=

# Booking mail (Resend). If RESEND_API_KEY is missing, bookSlot still succeeds.
RESEND_API_KEY=
NOTIFY_EMAIL=robertandrewbaker20@gmail.com
FROM_EMAIL=
