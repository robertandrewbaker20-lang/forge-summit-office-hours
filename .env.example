# Neon pooled connection string (Preview / Production / local). Never commit real values.
DATABASE_URL=

# Shared secret for /api/admin/* (Authorization: Bearer <key>)
ADMIN_API_KEY=

# Unlisted ops bookings page path token only: /ops/<OPS_SECRET>
# Do not use ?key= query strings.
OPS_SECRET=

# Booking mail (Resend). If RESEND_API_KEY is missing, bookSlot still succeeds.
RESEND_API_KEY=
NOTIFY_EMAIL=robertandrewbaker20@gmail.com
FROM_EMAIL=
